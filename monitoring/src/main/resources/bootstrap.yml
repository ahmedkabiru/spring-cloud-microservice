spring:
  application:
    name: monitoring
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true

turbine:
  appConfig: test-service
  clusterNameExpression: new String("default")
